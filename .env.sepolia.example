# Copy to .env.sepolia and fill in. NEVER commit the real file (see .gitignore).
SEPOLIA_RPC=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
DEPLOYER_KEY=0xYOUR_SEPOLIA_PRIVATE_KEY

# Filled in as you deploy (also paste into js/chain-config.js):
LIQUID_EDITION_ADDRESS=
SEASON_BALLOT_ADDRESS=
RENDER_CONTRACT_ADDRESS=

# For the Claude lore generator (scripts/generate-lore.mjs):
ANTHROPIC_API_KEY=sk-ant-...
